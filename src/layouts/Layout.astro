---
import "../styles/global.css";
import Nav from "../components/Nav.astro";
import LikeButton from "../components/LikeButton.astro";

interface Props {
  title: string;
  pageId?: string;
}

const { title, pageId } = Astro.props;
const currentPath = Astro.url.pathname;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" href="/favicon.ico" sizes="32x32" />
    <!--<link rel="icon" href="/icon.svg" type="image/svg+xml">-->
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" /><!-- 180×180 -->
    <link rel="manifest" href="/manifest.webmanifest" />
    <link
      rel="preload"
      href="/fonts/Inter/InterVariable.woff2"
      as="font"
      type="font/woff2"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/fonts/Inter/inter.css" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <Nav title={title} />
    <main>
      <div class="content">
        <slot />
        {
          pageId && (
            <div class="social">
              <LikeButton pageId={pageId} />
              <strong>
                ·{" "}
                <a href={Astro.url} class="link" id="copy-link">
                  Copy & Share:</a>
                {` sikorsky.design${currentPath}`}
              </strong>
            </div>
          )
        }
      </div>
    </main>
    <footer>
      <p class="secondary">© 2012&thinsp;–&thinsp;2024&nbsp;Paul Sikorsky</p>
    </footer>
  </body>
</html>

<style>
  .social {
    display: flex;
    align-items: center;

    margin-top: 56px;
  }
</style>

<script is:inline>
  document.getElementById('copy-link').addEventListener("click", (e) => {
    if (e.target.closest(".link")) {
      e.preventDefault();
      navigator.clipboard.writeText(
        `sikorsky.design${window.location.pathname}`
      );
    }
  });
</script>
